import 'package:flutter/material.dart';

class AppNavBarLogo extends StatelessWidget {
  const AppNavBarLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 80,
      width: 150,
      child: Text(
        "Hello",
      ),
    );
  }
}
