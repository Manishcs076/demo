import 'package:flutter/material.dart';

class ContactWatch extends StatelessWidget {
  const ContactWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
